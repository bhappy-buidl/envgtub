
# Application Downloader

This command-line tool allows you to download popular applications like Brave Browser, Google Chrome, Telegram, WhatsApp, and others with a simple command.

## Installation

To install the Application Downloader, follow these steps:

1. Install [Node.js](https://nodejs.org/) if you haven't already.
2. Clone or download this repository to your local machine.
3. Open a terminal or command prompt and navigate to the project directory.
4. Run `npm install` to install the required dependencies.

## Usage

To download an application, run the following command from the project directory:

##### high lvl overview of how app will work and what It aims to acomplish 1) go to a librairy>2) go to my single webapp landing page that insstructs instalation> 3) open my terminal on library machine>4) command a prompt tat installs a webappliscation(ex- "install brave" on comand line and that instalss brave browser onto the macins desktop)>


I want to be able to go to any machine and downlaod every app from one comand line


